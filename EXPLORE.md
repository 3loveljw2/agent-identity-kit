# EXPLORE.md · 自适应探索协议（路径 B）

> **当你不满足于"落地即用"，想让你的 Agent 找到"最适合你环境的最优路径"时，走这条路。**
> 给开发者的选择：路径 A = 复制即用（README 快速开始）；路径 B = 本协议，让 Agent 自适配你的环境。

## 为什么有这条路

通用技能包解决"大多数人"的问题，但**最优解永远属于具体环境**：你的设备、你的工具链、你的工作习惯、你的项目类型。路径 B 把"找最优"这件事交给 Agent——按协议扫描、试验、蒸馏，形成**你的个人化版本**。

## 协议总览（5 阶段）

```
阶段 0 环境扫描 → 阶段 1 匹配评估 → 阶段 2 小规模试验 → 阶段 3 蒸馏适配 → 阶段 4 持续进化
```

## 阶段 0 · 环境扫描（30 分钟内）

扫描并记录你的环境画像，输出到 `EXPLORE-REPORT.md`：

| 维度 | 要问的问题 | 记录 |
|---|---|---|
| 设备 | OS / CPU / 内存 / 是否移动端 | |
| 工具链 | Python 版本 / Node / 包管理器 / 编辑器 | |
| 项目类型 | CLI / Web / 数据 / 方法论 / 其他 | |
| 语言偏好 | 主要语言 / 代码风格 | |
| 工作习惯 | 个人还是团队 / 时间碎片化程度 / 英文水平 | |
| 约束 | 网络环境 / 可用 API / 数据敏感性 | |

## 阶段 1 · 匹配评估

对照 skills/ 的 10 个身份，输出评估表：

| 身份 | 环境匹配度 | 直接可用 | 需调整 | 不适用 |
|---|---|---|---|---|
| technical-architect | 高/中/低 | ✅/❌ | 调整点：___ | 原因：___ |
| ...（10 个全评） | | | | |

**评估标准**：该身份的核心能力是否与你的项目类型/工具链匹配；不匹配时是"调整可用"还是"本环境用不上"。

## 阶段 2 · 小规模试验（半天）

选 **1-2 个匹配度最高的身份**，用你的真实项目跑一个小任务：

```
试验记录模板（每条）：
- 身份：technical-architect
- 任务：为我的 X 项目做模块划分
- 输入：___ / 输出：___
- 结果：✅ 直接可用 / ⚠️ 有偏差（偏差：___）/ ❌ 不适用
- 调整建议：___
```

**纪律**：一次只试验 1-2 个身份；任务要小（半天内完成）；结果如实记录（包括失败）。

## 阶段 3 · 蒸馏适配（1-2 小时）

把试验结果写回**你的本地副本**（不修改上游仓库）：

1. 复制 skills/ 到你的项目（如 `my-agent-kit/`）
2. 按试验记录调整每个身份：
   - 改工作流程（适配你的工具链）
   - 改交付物模板（适配你的输出习惯）
   - 改验收标准（适配你的质量基线）
   - 加"环境备注"（你环境特有的注意事项）
3. 版本标记：`v0.1-local`（区别于上游 v0.x）

## 阶段 4 · 持续进化（你的 Agent 从此开始自成长）

```
用你的个人化技能包做任务 → 任务留痕（task-log.md）
→ 每满 10 条 → 蒸馏（聚类共性 → 更新本地 SKILL.md，版本 +1-local）
→ 同步更新 EXPLORE-REPORT.md（环境画像随使用修正）
→ 你的技能包越用越适配你
```

## 输出物

- `EXPLORE-REPORT.md`：环境画像 + 匹配评估 + 试验记录 + 个人化版本说明
- `my-agent-kit/`（本地）：个人化的技能包副本（可 git 版本化，是你的资产）
- 可选：把你适配出的优秀调整**贡献回上游**（提 PR / 记录到 EVOLUTION.md）——帮助其他环境相似的人

## 与路径 A 的关系

- **路径 A**（落地即用）：复制 → 立即工作 → 通用最优
- **路径 B**（本协议）：扫描 → 试验 → 蒸馏 → **你的环境最优**
- 两条路共享同一套方法论（10 身份能力不变），区别只在"交付后是否再适配"
- **建议**：先用 A 跑通一次，再决定要不要走 B 深挖——A 是快车道，B 是定制化
